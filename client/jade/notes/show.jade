h1
  | {{ ctrl.note.name }} 
  .btn-group
    a.btn.btn-default(ng-href="#/notes/{{ctrl.note.id}}/edit") Edit
    a.btn.btn-danger(ng-click="ctrl.destroyNote()") Delete
span.label.label-default {{ ctrl.note.visibility }}
p {{ ctrl.note.body }}
